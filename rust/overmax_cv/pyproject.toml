[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "overmax-cv"
version = "0.1.0"
requires-python = ">=3.10"
description = "Small native image feature backend for Overmax"

[tool.maturin]
module-name = "_overmax_cv"
python-source = "python"
