[package]
name = "overmax_cv"
version = "0.1.0"
edition = "2021"

[lib]
name = "_overmax_cv"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
