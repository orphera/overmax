# Capture & Window Management Decision Log

화면 캡처(GDI, DXGI, Linux), 윈도우 추적(WindowTracker), DWM 컴포지터 상호작용, 창 드래그/오버레이 가시성 관련 주요 설계 결정의 배경과 이유를 기록한다.

---

## 📋 Decision History

| 날짜 | 결정 | 이유 | 참조 |
|:---|:---|:---|:---|
| 2026-05 | GDI 캡처 버퍼 인플레이스 재사용 | CPU 프로파일링 결과 memcpy 힙 할당이 주 병목 | [cpu-optimization-message-pump.md](../2026-05-24-cpu-optimization-message-pump.md) |
| 2026-05 | WindowTracker 동적 폴링 주기 | win32u 시스템 콜 오버헤드 해소 | [cpu-optimization-message-pump-review.md](../2026-05-24-cpu-optimization-message-pump-review.md) |
| 2026-07-09 | NativeApp::update 330줄 분할 및 락 오버헤드 최적화 | 자이언트 함수를 3개 헬퍼 함수로 분해하고 `game_rect` 락 획득을 1회로 병합 | [native_app_viewports.rs](../../rust/overmax_app/src/ui/native_app_viewports.rs) |
| 2026-07-13 | 캡처 FPS 및 GUI 렌더링 스팸 최적화 | ScreenCaptureSettings에 active_sleep_ms/background_sleep_ms를 추가해 분석 주기를 유연하게 설정하고, 마우스 오버 시 실제 이동/드래그가 발생할 때만 request_repaint()를 호출하여 무의미한 CPU/GPU 낭비 억제 | [settings.rs](../../rust/overmax_data/src/config/settings.rs) / [native_app_viewports.rs](../../rust/overmax_app/src/ui/native_app_viewports.rs) / [detection_worker.rs](../../rust/overmax_engine/src/detector/detection_worker.rs) |
| 2026-08-08 | 오버레이 항상 표시 및 DXGI/GDI 캡처 설정 옵션화, 디버그 실시간 대시보드 구축 | 씬 미인식(`SceneType::Unknown`) 또는 특정 유저 포커스 환경에 따른 오버레이 미노출 이슈를 완벽히 해결하고, 캡처 백엔드 선택권(`capture.engine`), Content Protection 선택권(`capture.content_protected`), 오버레이 항시 표시(`overlay.always_visible`), 그리고 100ms 실시간 App State 디버그 대시보드를 제공하여 빠른 원인 진단 및 사용자 편의성 확보 | [settings.rs](../../rust/overmax_data/src/config/settings.rs) / [settings_ui.rs](../../rust/overmax_app/src/ui/settings_ui.rs) / [debug_ui.rs](../../rust/overmax_app/src/ui/debug_ui.rs) / [native_app_viewports.rs](../../rust/overmax_app/src/ui/native_app_viewports.rs) |
| 2026-08-09 | DWM Z-order 가림 해제 (`GWL_HWNDPARENT` 소거) 및 Win32 ReleaseCapture + WM_NCLBUTTONDOWN OS 네이티브 드래그 완성 | DWM이 오버레이 창을 게임 뒤로 가려버리던 `GWL_HWNDPARENT` 소유권을 전면 제거하고, 드래그 시 `ReleaseCapture` 및 `WM_NCLBUTTONDOWN (HTCAPTION)` 신호를 주입하여 144Hz/240Hz OS 네이티브 윈도우 이동(`SC_MOVE`) 모달 루프에 위임함으로써 버벅임 0%, 덜덜 떨림 0%의 완벽한 오버레이 창 이동을 구현함 | [native_app_viewports.rs](../../rust/overmax_app/src/ui/native_app_viewports.rs) / [overlay_header.rs](../../rust/overmax_app/src/ui/components/overlay_header.rs) / [lite_panel.rs](../../rust/overmax_app/src/ui/components/lite_panel.rs) |
| 2026-08-10 | Capture Backend 런타임 실시간 스위칭 및 GDI 기본값 전환 | 설정 UI에서 Capture Backend(GDI/DXGI) 변경 시 디텍션 워커가 런타임에 즉시 engine을 스위칭하도록 동기화하고, 안정성 중심의 GDI 백엔드를 기본값으로 전환 | [detection_worker.rs](../../rust/overmax_engine/src/detector/detection_worker.rs) / [windows/mod.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/mod.rs) / [settings_ui.rs](../../rust/overmax_app/src/ui/settings_ui.rs) |
| 2026-08-10 | DXGI 백엔드 다중 모니터 Output 동적 탐색 및 로컬 오프셋 변환 지원 | DXGI Desktop Duplication 사용 시 서브 모니터에서 게임 구동 시 디텍션이 동작하지 않던 문제를 해결하기 위해, 게임 창 위치에 따른 DXGI Output1 동적 탐색 및 캡처 크롭 오프셋 변환 구현 | [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-08-15 | DXGI vs GDI 캡처 정량 실측 및 Multi-Monitor Adaptive Capture Selector 적용 | GDI 대비 DXGI 캡처 소요시간이 ~30ms -> ~4ms로 6배 이상(85% 절감) 단축됨을 실측하고, Auto 옵션 시 단일모니터=DXGI / 멀티모니터=GDI로 자동 선택되도록 구현 | [mod.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/mod.rs) / [settings_ui.rs](../../rust/overmax_app/src/ui/settings_ui.rs) |
| 2026-08-15 | GDI BitBlt CAPTUREBLT 플래그 소거 및 DXGI 예외 처리 강화 | DWM 락을 유발하던 CAPTUREBLT를 제거하고 DXGI ACCESS_LOST/Timeout 및 3초 GDI Fail-Safe 폴백 안정화 | [gdi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/gdi.rs) / [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-08-16 | 텔레메트리 파일 로깅의 `telemetry` Cargo Feature 분리 및 세션 자동 롤링 | 릴리스 빌드에서 파일 쓰기를 기본 소거(`#[cfg(debug_assertions)]`)하되 벤치마크 시 `--features telemetry`로 활성화할 수 있도록 분리하고, 신규 게임 기동 시 `telemetry.prev.log`로 자동 롤링 백업 구현 | [Cargo.toml](../../rust/overmax_engine/Cargo.toml) / [detection_worker.rs](../../rust/overmax_engine/src/detector/detection_worker.rs) |
| 2026-08-17 | Windows 11 DWM 캡션/테두리 제거(`WM_NCCALCSIZE` 서브클래싱) 및 픽셀 완벽 스크린 앵커 드래그 구축 | DWM 투명 확장 시 비클라이언트 캡션 버튼("- ㅁ X") 및 1px 상단 테두리 선이 노출되던 문제를 `SetWindowSubclass`로 `WM_NCCALCSIZE`를 가로채 100% 영구 소멸시키고, 드래그 처리를 마우스 스크린 절대 좌표 기반 `handle_screen_drag`로 모듈화하여 타이틀바 깜빡임 없는 완벽한 1:1 창 이동 및 플랫폼 캡슐화 달성 | [windows.rs](rust/overmax_app/src/ui/platform/windows.rs) / [native_app_viewports.rs](rust/overmax_app/src/ui/native_app_viewports.rs) |
| 2026-09-03 | 오버레이 뷰포트 크기 보존(1x1 축소 임시방편 제거) 및 플랫폼 간 텔레메트리/워커 생명주기 일원화 | 오버레이 숨김(`SWP_HIDEWINDOW`) 시 뷰포트를 1x1로 축소하던 구형 덕트테이프 코드를 제거하여 장면 전환 시 DXGI 스왑체인 해제/재생성으로 인한 깜빡임/버벅임을 영구 제거하고, `RepaintFingerprint`, `RuntimeTelemetry`, `DetectionWorker::spawn`, `detecting_output` 팩토리를 크로스 플랫폼으로 통일하여 Windows/Linux 간 상태 전이 및 텔레메트리 전달 정합성을 완성함 | [native_app_viewports.rs](rust/overmax_app/src/ui/native_app_viewports.rs) / [detection_worker.rs](rust/overmax_engine/src/detector/detection_worker.rs) / [telemetry.rs](rust/overmax_engine/src/detector/telemetry.rs) |
| 2026-09-03 | DXGI Desktop Duplication HDR 자동 변환(`IDXGIOutput5`) 및 듀얼 GPU 어댑터 자동 탐색 도입 | HDR 디스플레이 환경에서 DXGI 포맷 불일치로 인한 검은 화면/GDI 폴백을 원천 차단하기 위해 `IDXGIOutput5::DuplicateOutput1`을 적용하여 OS DWM 하드웨어 레벨에서 8비트 SDR(B8G8R8A8)로 0ms 자동 변환을 구현하고, `CreateDXGIFactory1`을 통해 활성 디스플레이 출력이 연결된 외장 GPU(dGPU)를 우선 바인딩하여 7800X3D 등 CPU 내장 그래픽(iGPU)과의 어댑터 충돌을 영구 소멸시킴 | [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-09-04 | GPU ROI Atlas 512x512 도입 및 1080p 제로 코스트 DMA 바이패스(Adaptive Normalization) 결정 | 실측 결과 전체 캡처 시간의 절반 이상(2.62ms)이 Map/memcpy 대역폭 병목임을 확인하고 512x512 아틀라스 도입(0.72ms, 1.9ms 절감)을 확정함. 1080p 16:9 환경에서는 GPU Draw Quad를 완전 바이패스하여 백버퍼 -> VRAM Staging으로 순수 `CopySubresourceRegion` 43회 직행(Zero Draw Call)하고, 비-1080p 환경에만 선별적 16:9 UV Draw Quad 안전망을 가동하는 2-Track 파이프라인 채택 | [gpu-roi-atlas-capture-architecture.md](../plans/2026-09-04-gpu-roi-atlas-capture-architecture.md) |
| 2026-09-04 | 자립형 아틀라스 슬롯 확장(카테고리 띠 64x60 / 마진 22x96) 및 DXGI 하드웨어 캡처 연동 (Step 3) | 아틀라스 모드에서 씬 판독(`parse_static_scene`)이 풀 프레임 캡처 없이 완전 자립 동작하도록 자켓 슬롯 폭을 64로 확장(우측 카테고리 띠 4px 포함)하고 mode_colorbar에 8px 마진을 포함하여 43개 슬롯 전원을 512x512 내에 100% 무손실 패킹 완료함. DXGI Staging Texture와 백버퍼 간 43회 `CopySubresourceRegion` 직행 및 단일 1회 1MB memcpy 전송 파이프라인 완성 | [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) / [atlas_layout.rs](../../rust/overmax_engine/src/detector/atlas_layout.rs) / [roi.rs](../../rust/overmax_engine/src/detector/roi.rs) |
| 2026-09-04 | 비-1080p 해상도 지원을 위한 조건부 GPU Normalizer (Draw Quad) 파이프라인 연동 (Step 4) | 1440p, 4K, 16:10(Steam Deck), 21:9(울트라와이드) 환경에서 발생하는 14MB~33MB PCIe 대역폭 폭탄을 차단하기 위해 D3D11 기반 풀스크린 트라이앵글 셰이더(VS/PS 바이트코드 내장) 및 Bilinear Sampler를 구현함. 1080p 화면은 0ms Fast Path로 직행하고, 비-1080p 화면만 1회 단일 패스(Draw 3)로 16:9 영역을 1920x1080 Render Target으로 Bilinear 정규화한 뒤 43개 아틀라스 슬롯으로 고속 복사하여 4K에서도 CPU 전송량을 단 1MB로 엄격 고정함 | [normalizer.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/normalizer.rs) / [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-09-04 | GPU ROI Atlas 핑퐁 더블 버퍼링(Double-Buffered Staging Textures) 및 Flush() 비동기 파이프라인 구축 (실측 입증 완료) | 실전 게임플레이 벤치마크 결과 5.12ms 캡처 지연의 85% 이상(~4.5ms)이 동기식 context.Map 호출에 따른 GPU 파이프라인 스톨(Wait for GPU)임을 규명함. 512x512 아틀라스(1MB) 특성을 활용하여 2개의 Staging 텍스처를 교대로 사용하는 핑퐁 더블 버퍼링을 구현함. 새 프레임 획득 시 GPU 복사 후 Flush()로 즉시 비동기 DMA 전송을 시작하고 CPU는 직전 틱에 완료된 버퍼를 즉시 Map하여 GPU 동기화 대기를 0ms로 소거함. 실전 인게임 플레이 벤치마크 결과 DXGI 캡처 지연이 6.21ms ➔ 0.663ms(P50: 0.627ms)로 89.3% 수직 단축(9.4배 고속화)되고 결과창 인식률 100%가 유지됨을 최종 실측 검증 완료 | [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-09-04 | 캡처 고속화 A/B/C 3단계 실측 기여도 완전 분리 검증 (더블버퍼링 34.3% vs 아틀라스 65.7%) | 더블버퍼링 도입 후 극적인 성능 향상(9.4배)에 대해 [A] main(단일 1080p, 4.50ms), [B] fullframe-db(더블 1080p, 3.17ms), [C] atlas-db(더블 512x512, 0.62ms) 3단계 실전 플레이 데이터를 완전히 분리 계측함. 더블버퍼링 단독으로는 1080p(8.3MB) 복사 비용으로 인해 3.17ms 한계에 봉착하지만(-1.33ms, 34.3% 기여), 512x512 아틀라스가 메모리 복사량을 87.5% 절감하여(-2.55ms 추가, 65.7% 기여) 비로소 0.62ms의 서브밀리초 진입이 가능함을 정량 증명함. 또한 결과창 인식률 개선(12/19 ➔ 19/19)은 캡처 지연이 아닌 커밋 8288bf1(숫자 1 분할 버그 수정)의 순수 기여임을 명확히 규명함 | [gpu-roi-atlas-capture-architecture.md](../plans/2026-09-04-gpu-roi-atlas-capture-architecture.md) |
| 2026-09-07 | DXGI HDR(scRGB) 진단 파이프라인 모듈화 및 Shift+F11 RAW 프레임 덤퍼 구축 | HDR(scRGB) 환경에서의 디텍션 왜곡 원인 규명(색역 변환 및 Auto HDR 비선형성)을 위한 오프라인 재현 환경을 마련하고자, 개발자가 튜닝한 FP16 디코딩 및 정규화 로직을 `hdr_pipeline.rs`로 격리하고 인게임 곡 목록(Freestyle) 등 원하는 순간에 `Shift+F11` 단축키로 512x512 원본 FP16 바이트를 `cache/hdr_snapshot.raw`로 덤프(MessageBeep 알림음 피드백 포함)하는 진단 파이프라인과 `hdr_replay_test.rs` 오프라인 검증 테스트 하네스를 완성함 | [hdr_pipeline.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/hdr_pipeline.rs) / [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) / [hdr_replay_test.rs](../../rust/overmax_engine/tests/hdr_replay_test.rs) |
| 2026-09-07 | Win32 CCD API 기반 SDR White Level 동적 자동 감지 및 무설정 다중 모니터 대응 구축 | 모니터 및 Windows HDR 설정('SDR 콘텐츠 밝기' 슬라이더)에 따라 SDR 백색 배율이 1.0(80 nits)~7.5(600 nits)로 달라지는 문제를 해결하기 위해, Win32 Connecting and Configuring Displays (CCD) API(`DisplayConfigGetDeviceInfo(DISPLAYCONFIG_DEVICE_INFO_GET_SDR_WHITE_LEVEL)`)를 통해 DXGI 출력 디바이스의 실제 SDR 화이트 레벨을 자동 감지하고 이에 맞춰 64KB LUT를 단 0.05ms만에 동적 생성하도록 구현함. 비선형 Reinhard 톤매핑의 계조 왜곡(중간톤 폭발로 인한 자켓/씬 인식 실패)을 실측으로 증명 및 배제하고, 무설정(Zero-Config) 자동 감지 + `settings.json` 수동 오버라이드 + 5.168 안전 폴백 체계를 완성함 | [hdr_pipeline.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/hdr_pipeline.rs) / [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) / [settings.rs](../../rust/overmax_data/src/config/settings.rs) |
| 2026-09-07 | DXGI 1.6 IDXGIOutput6 기반 실시간 모니터 색역(ColorSpace) 사전 판별 및 SDR/HDR 최적 포맷 분기 구축 | DuplicateOutput1의 포맷 협상 루프에서 FP16이 우선될 경우 SDR 모니터도 FP16으로 강제 변환 수신되어 SDR 인식이 깨지고, 반대로 B8G8R8A8이 우선될 경우 HDR 모니터가 DWM 톤매핑으로 수신되어 왜곡되는 딜레마를 해결하기 위해, DXGI 1.6 `IDXGIOutput6::GetDesc1()`을 통해 디스플레이의 실제 ColorSpace(G2084/G10/G22) 및 비트 심도를 0ms만에 사전 판별하도록 구현함. SDR 모니터는 네이티브 B8G8R8A8_UNORM(변환 오버헤드 0%, 1:1 완벽 인식), HDR 모니터는 scRGB R16G16B16A16_FLOAT(64KB 무손실 선형 LUT 복원)을 각각 1순위로 협상하도록 분기하여 무설정(Zero-Config) 양방향 완벽 호환성을 완성함 | [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |
| 2026-09-09 | DXGI HDR(scRGB) DCI-P3 광색역 역변환 및 음수 클리핑 제거 (0.1ms 고속화) | DJMAX 게임 엔진이 HDR 환경에서 DCI-P3(D65) 원색으로 렌더링함에 따라 scRGB 음수 채널 발생 시 clamp(0)으로 인한 색상 파괴를 방지하기 위해, M_709_TO_P3 역변환 행렬 및 inv_scale 사전 곱셈을 적용하여 루프 내 나눗셈 제거 및 0.1ms 미만 초고속 BGRA8 복원 달성 | [hdr_pipeline.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/hdr_pipeline.rs) / [dxgi.rs](../../rust/overmax_engine/src/capture/capture_engine/windows/dxgi.rs) |






